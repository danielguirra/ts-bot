// const fs = require('fs');
// const dotenv = require('dotenv');

// function convertEnvToJson(filePath) {
//    // Carregar o arquivo .env
//    const envConfig = dotenv.parse(fs.readFileSync(filePath));

//    // Converter o objeto em JSON
//    const jsonData = JSON.stringify(envConfig, null, 2);

//    // Retornar o JSON
//    return jsonData;
// }

// // Exemplo de uso
// const envFilePath = './.env';
// const jsonData = convertEnvToJson(envFilePath);

// fs.writeFileSync('envs.json', JSON.stringify(jsonData),)
